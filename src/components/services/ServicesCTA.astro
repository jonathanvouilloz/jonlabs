---
// Section CTA finale de la page services
// Invitation à passer à l'action
import svgPinkStar from '../../assets/pink-star.svg';
---

<section class="services-cta">
  <!-- Decoration -->
  <img
    src={svgPinkStar.src}
    alt=""
    class="cta-star"
    aria-hidden="true"
  />

  <div class="cta-content">
    <h2 class="cta-title">
      Prêt à
      <span class="highlight">démarrer</span>
      ?
    </h2>

    <p class="cta-text">
      Pas de formulaire à rallonge. Discutons de ton projet et je te dis
      honnêtement si je peux t'aider.
    </p>

    <div class="cta-buttons">
      <a href="/contact" class="btn-primary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 11.5a8.38 8.38 0 01-.9 3.8 8.5 8.5 0 01-7.6 4.7 8.38 8.38 0 01-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 01-.9-3.8 8.5 8.5 0 014.7-7.6 8.38 8.38 0 013.8-.9h.5a8.48 8.48 0 018 8v.5z"/>
        </svg>
        Discutons de ton projet
      </a>

      <a href="mailto:hello@jonlabs.ch" class="btn-email">
        <span class="email-prompt">→</span>
        hello@jonlabs.ch
      </a>
    </div>

    <!-- Trust indicators -->
    <div class="trust-indicators">
      <div class="indicator">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 11-5.93-9.14"/>
          <path d="M22 4L12 14.01l-3-3"/>
        </svg>
        <span>Réponse sous 24h</span>
      </div>
      <div class="indicator">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 11-5.93-9.14"/>
          <path d="M22 4L12 14.01l-3-3"/>
        </svg>
        <span>Premier appel gratuit</span>
      </div>
      <div class="indicator">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 11-5.93-9.14"/>
          <path d="M22 4L12 14.01l-3-3"/>
        </svg>
        <span>Devis clair et détaillé</span>
      </div>
    </div>
  </div>
</section>

<style>
  .services-cta {
    position: relative;
    padding: 5rem 1rem;
    background: #0a0a0f;
    color: white;
    text-align: center;
    overflow: hidden;
  }

  /* Grid pattern - style hero */
  .services-cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    pointer-events: none;
  }

  /* Gradient overlay */
  .services-cta::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 30% 50%, rgba(0, 217, 163, 0.08) 0%, transparent 50%),
      radial-gradient(circle at 70% 50%, rgba(163, 0, 217, 0.08) 0%, transparent 50%);
    pointer-events: none;
  }

  /* Decoration star */
  .cta-star {
    position: absolute;
    top: 2rem;
    right: 10%;
    width: 50px;
    opacity: 0.6;
    animation: spin 20s linear infinite;
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 700;
    margin: 0 0 1.25rem;
    line-height: 1.2;
  }

  .highlight {
    position: relative;
    display: inline-block;
  }

  .highlight::after {
    content: '';
    position: absolute;
    left: -4px;
    right: -4px;
    bottom: 4px;
    height: 30%;
    background: var(--blue, #00D9A3);
    z-index: -1;
    opacity: 0.8;
  }

  .cta-text {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 0 2rem;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    margin-bottom: 2.5rem;
  }

  @media (min-width: 480px) {
    .cta-buttons {
      flex-direction: row;
      justify-content: center;
    }
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    padding: 1rem 1.5rem;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s ease-out;
    background: rgba(0, 217, 163, 0.15);
    color: white;
    border: 1px solid rgba(0, 217, 163, 0.5);
    backdrop-filter: blur(4px);
  }

  .btn-primary:hover {
    background: rgba(0, 217, 163, 0.25);
    border-color: rgba(0, 217, 163, 0.8);
  }

  /* Style terminal pour email */
  .btn-email {
    display: inline-flex;
    align-items: center;
    font-family: 'Plus Jakarta Sans', monospace;
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.95rem;
    letter-spacing: 0.02em;
    text-decoration: none;
    transition: color 0.2s;
    padding: 1rem 0;
  }

  .btn-email:hover {
    color: var(--blue, #00D9A3);
  }

  .email-prompt {
    color: var(--blue, #00D9A3);
    margin-right: 0.5rem;
    font-weight: 600;
  }

  /* Trust indicators */
  .trust-indicators {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
  }

  .indicator {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .indicator svg {
    color: var(--blue, #00D9A3);
  }

  /* Responsive */
  @media (max-width: 480px) {
    .services-cta {
      padding: 4rem 1rem;
    }

    .cta-title {
      font-size: 1.75rem;
    }

    .btn-primary {
      width: 100%;
      justify-content: center;
    }

    .btn-email {
      justify-content: center;
    }

    .trust-indicators {
      flex-direction: column;
      gap: 0.75rem;
    }
  }
</style>
