---
import "../styles/global.css";
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { getWebPageSchema, getBreadcrumbSchema, getFAQPageSchema, getServiceItemSchema } from "../lib/seo";
import { SITE_URL, localBusinessData } from "../data/schema";

// ============================================
// SEO META
// ============================================
const pageUrl = `${SITE_URL}/hermes`;
const pageTitle = "Hermès — agent IA pour PME romandes | Jon Labs";
const metaDescription = "Hermès : agent IA prêt à déployer pour ta PME romande. Tri d'e-mails, relances, préparation de documents. Prix CHF affichés, cadrage rapide, conformité nLPD.";

// ============================================
// FAQ (locale)
// ============================================
const faqs = [
  {
    question: "Hermès, c'est un produit fini ou un développement sur mesure ?",
    answer: "Les deux à la fois. Hermès est un agent IA que j'ai déjà éprouvé sur des tâches concrètes de PME (tri d'e-mails, relances, préparation de documents). On part de cette base éprouvée, puis on le branche sur tes outils et tes process. Tu ne payes pas six mois de R&D : tu récupères un agent qui tourne, adapté à ton contexte."
  },
  {
    question: "Combien ça coûte, concrètement ?",
    answer: "Un cadrage + un agent simple part de CHF 2'500 à 4'000 selon le nombre de tâches. Un agent métier intégré à plusieurs outils (CRM, boîte mail, facturation) se situe entre CHF 6'000 et 15'000. À cela s'ajoute un coût récurrent de CHF 50 à 150 par mois (hébergement + API du modèle de langage). Tout est affiché sur cette page, sans devis-surprise."
  },
  {
    question: "Combien de temps avant que l'agent tourne ?",
    answer: "Compte deux à trois semaines entre le cadrage et un premier agent en production sur une tâche précise. Pas un chantier de six mois : on vise un résultat mesurable rapidement, puis on élargit le périmètre si ça vaut le coup."
  },
  {
    question: "Est-ce que l'agent va envoyer des choses tout seul à mes clients ?",
    answer: "Seulement si tu le décides. Par défaut, sur tout ce qui a un enjeu (e-mails clients, devis, relances), l'agent prépare et tu valides avant l'envoi. La validation humaine est un garde-fou câblé dès la configuration, pas une option qu'on ajoute après coup."
  },
  {
    question: "Mes données restent-elles en Suisse et conformes nLPD ?",
    answer: "On cadre dès le départ ce qui sort de chez toi et ce qui reste. Hébergement conforme, minimisation des données envoyées au modèle, et documentation de ce qui transite. Je travaille depuis Genève et je connais les contraintes des PME romandes sur ce point."
  },
  {
    question: "Quelle différence avec le guide gratuit sur Hermès ?",
    answer: "Le guide explique ce qu'est l'agent, ce qu'il fait et ce qu'il coûte si tu l'installes toi-même. Cette page-ci, c'est l'offre : je m'occupe du cadrage, de l'intégration, des garde-fous et de la prise en main pour que tu n'aies pas à devenir technique. Le guide informe, l'offre livre."
  }
];

// ============================================
// SCHEMA.ORG
// ============================================
const webPageSchema = getWebPageSchema({
  url: pageUrl,
  name: pageTitle,
  description: metaDescription
});

const breadcrumbSchema = getBreadcrumbSchema([
  { name: "Accueil", url: SITE_URL },
  { name: "Consultant IA", url: `${SITE_URL}/consultant-ia` },
  { name: "Hermès", url: pageUrl }
]);

const faqSchema = getFAQPageSchema(faqs);
const serviceSchema = getServiceItemSchema("hermes");

const schemas = [
  webPageSchema,
  breadcrumbSchema,
  faqSchema,
  ...(serviceSchema ? [serviceSchema] : []),
  localBusinessData
];

// ============================================
// TRUST LINE (hero)
// ============================================
const preuvesHero = ["Basé à Genève", "Prix en CHF", "Conforme nLPD"];

// ============================================
// CE QUE FAIT HERMÈS (capacités)
// ============================================
const capacites = [
  {
    icon: "ri-mail-settings-line",
    titre: "Tri et priorisation des e-mails",
    description: "Il lit ta boîte de réception, repère ce qui est prioritaire et prépare des réponses contextualisées en tenant compte de l'historique. Tu gardes la main sur l'envoi."
  },
  {
    icon: "ri-mail-send-line",
    titre: "Relances clients autonomes",
    description: "Il suit l'état de tes devis ouverts et génère des relances personnalisées selon le contexte (date de l'offre, montant, historique). Zéro devis oublié."
  },
  {
    icon: "ri-file-text-line",
    titre: "Préparation de documents",
    description: "À partir du contexte d'un prospect et de tes offres, il structure une proposition commerciale en quelques minutes. Un premier draft au lieu d'une page blanche."
  },
  {
    icon: "ri-customer-service-2-line",
    titre: "Réponses de premier niveau",
    description: "Il traite les demandes récurrentes et les questions simples, et fait remonter à toi ce qui demande un vrai jugement. Ton temps va où il compte."
  },
  {
    icon: "ri-search-eye-line",
    titre: "Veille continue",
    description: "Il surveille l'activité de tes concurrents ou de ton marché et t'envoie un résumé prêt à lire chaque semaine. 2 à 3 heures récupérées sur la veille manuelle."
  },
  {
    icon: "ri-plug-line",
    titre: "Intégration à tes outils",
    description: "On le branche sur ce que tu utilises déjà (boîte mail, CRM, facturation) plutôt que de tout remplacer. La mémoire persistante lui évite de repartir de zéro à chaque fois."
  }
];

// ============================================
// CAS D'USAGE CONCRETS
// ============================================
const casUsage = [
  {
    icon: "ri-scales-3-line",
    secteur: "Fiduciaire à Lausanne",
    description: "Relances automatiques des pièces manquantes avant les décomptes, avec un résumé hebdomadaire des dossiers en retard. Le collaborateur valide, l'agent rédige."
  },
  {
    icon: "ri-megaphone-line",
    secteur: "Agence de communication à Genève",
    description: "Veille concurrentielle sur cinq comptes clients et pré-rédaction des propositions commerciales. Le premier draft passe d'une heure à dix minutes."
  },
  {
    icon: "ri-building-line",
    secteur: "Régie immobilière",
    description: "Tri des e-mails entrants par urgence et préparation des réponses de premier niveau aux locataires. Les cas sensibles remontent directement au gestionnaire."
  },
  {
    icon: "ri-store-2-line",
    secteur: "PME de services (5-15 personnes)",
    description: "Suivi des devis ouverts et relances contextualisées selon l'historique client. Le dirigeant approuve avant chaque envoi, rien ne part sans son feu vert."
  }
];

// ============================================
// L'OFFRE — grille de prix CHF
// ============================================
const paliers = [
  {
    nom: "Cadrage + agent simple",
    prix: "CHF 2'500 – 4'000",
    tag: "Pour démarrer",
    highlight: false,
    pour: "Une tâche précise qui te mange du temps chaque semaine.",
    inclus: [
      "Audit de tes process et repérage de la tâche à automatiser",
      "Un agent branché sur un outil (boîte mail ou CRM)",
      "Garde-fous et validation humaine sur les envois",
      "Prise en main de ton équipe"
    ]
  },
  {
    nom: "Agent métier intégré",
    prix: "CHF 6'000 – 15'000",
    tag: "Le plus demandé",
    highlight: true,
    pour: "Plusieurs tâches et plusieurs outils à connecter.",
    inclus: [
      "Agent multi-tâches connecté à tes outils (CRM, mail, facturation)",
      "Enchaînements automatiques et mémoire persistante",
      "Cadrage nLPD : ce qui sort, ce qui reste chez toi",
      "Tests en conditions réelles avant mise en production",
      "Formation et documentation pour piloter en autonomie"
    ]
  },
  {
    nom: "Fonctionnement mensuel",
    prix: "CHF 50 – 150 / mois",
    tag: "Récurrent",
    highlight: false,
    pour: "Ce que coûte l'agent une fois en route.",
    inclus: [
      "Hébergement de l'agent (VPS ou serveur)",
      "API du modèle de langage, facturée à l'usage",
      "Fourchette basse pour un volume PME classique",
      "Sans engagement de durée"
    ]
  }
];

// ============================================
// COMMENT ÇA SE PASSE — étapes
// ============================================
const etapes = [
  {
    step: "01",
    title: "Cadrage",
    description: "On regarde tes process réels et on repère les 2-3 tâches qui pèsent le plus. Je te dis franchement où Hermès aide et où il n'apporte rien. Pas de promesse en l'air."
  },
  {
    step: "02",
    title: "Intégration",
    description: "Je branche l'agent sur tes outils et je le configure sur la première tâche. Un résultat en production en deux à trois semaines, pas un chantier interminable."
  },
  {
    step: "03",
    title: "Garde-fous",
    description: "Validation humaine sur ce qui l'exige, cadrage des données (ce qui sort, ce qui reste chez toi), hébergement conforme nLPD. L'agent ne décide pas à ta place."
  },
  {
    step: "04",
    title: "Prise en main",
    description: "Je forme ton équipe à l'usage quotidien. L'objectif n'est jamais de te rendre dépendant : c'est de te donner un outil qui tourne et que tu pilotes."
  }
];

// ============================================
// MAILLAGE
// ============================================
const liens = [
  {
    icon: "ri-brain-line",
    title: "Consultant IA (hub)",
    description: "La page de référence sur le conseil IA pour PME romandes : méthode, types d'agents, conformité et tarifs.",
    href: "/consultant-ia",
    cta: "Découvrir"
  },
  {
    icon: "ri-book-open-line",
    title: "Le guide complet sur Hermès",
    description: "Ce qu'est l'agent, ce qu'il fait, ses limites et ce qu'il coûte si tu l'installes toi-même. Le regard terrain, sans hype.",
    href: "/blog/hermes-agent-ia-pme",
    cta: "Lire le guide"
  },
  {
    icon: "ri-terminal-box-line",
    title: "OpenClaw pour PME",
    description: "L'autre agent IA open source, orienté usage individuel et mobile. Comparaison honnête avec Hermès.",
    href: "/blog/openclaw-pme-suisse",
    cta: "Lire l'article"
  },
  {
    icon: "ri-flow-chart",
    title: "Workflows vs agents IA",
    description: "Quand un simple automatisme suffit, et quand il faut vraiment un agent qui raisonne. Pour choisir juste.",
    href: "/blog/workflows-vs-agents-ia-pme",
    cta: "Lire l'article"
  }
];
---

<Layout
  title={pageTitle}
  description={metaDescription}
  schemas={schemas}
>
  <Header />

  <!-- ============================================== -->
  <!-- HERO -->
  <!-- ============================================== -->
  <section class="hero-section -mt-[120px] pt-[150px] pb-16 md:pt-[160px] md:pb-20 px-4 relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
      <div class="absolute top-0 -left-20 w-[500px] h-[500px] bg-[var(--blue)] opacity-20 blur-[120px] rounded-full"></div>
      <div class="absolute top-1/3 -right-20 w-[400px] h-[400px] bg-[var(--violet)] opacity-15 blur-[100px] rounded-full"></div>
      <div class="absolute bottom-0 left-1/3 w-[300px] h-[300px] bg-[var(--blue)] opacity-10 blur-[80px] rounded-full"></div>
    </div>

    <div class="max-w-4xl mx-auto relative z-10 text-center">
      <div class="flex flex-wrap justify-center gap-3 mb-8">
        <span class="inline-flex items-center gap-2 bg-white border-2 border-black px-3 py-1 text-xs font-mono shadow-[2px_2px_0px_rgba(0,0,0,1)]">
          <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
          Disponible
        </span>
        <span class="inline-flex items-center gap-2 bg-[var(--blue)] border-2 border-black px-3 py-1 text-xs font-mono shadow-[2px_2px_0px_rgba(0,0,0,1)]">
          <i class="ri-robot-2-line"></i>
          Agent IA · prêt à déployer
        </span>
      </div>

      <h1 class="text-4xl md:text-6xl font-normal tracking-tight mb-6 leading-[1.05]">
        Hermès
      </h1>

      <p class="text-lg md:text-2xl text-gray-700 mb-8 leading-relaxed max-w-3xl mx-auto">
        L'agent IA qui prend en charge tes tâches répétitives — tri d'e-mails, relances, préparation de documents — et te rend le temps que tu passes à les faire à la main.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-10">
        <a
          href="/contact"
          class="bg-[var(--blue)] border-2 border-black px-6 py-4 text-lg font-bold shadow-[4px_4px_0px_rgba(0,0,0,1)] transition-all duration-200 hover:bg-[var(--yellow)] hover:shadow-[2px_2px_0px_rgba(0,0,0,1)] hover:translate-x-[2px] hover:translate-y-[2px] flex items-center justify-center gap-2"
        >
          <i class="ri-calendar-check-line"></i>
          Cadrer mon déploiement
        </a>
        <a
          href="/blog/hermes-agent-ia-pme"
          class="bg-white border-2 border-black px-6 py-4 text-lg font-medium transition-all duration-200 hover:bg-gray-100 flex items-center justify-center gap-2"
        >
          Lire le guide complet
          <i class="ri-arrow-right-line"></i>
        </a>
      </div>

      <div class="flex flex-wrap justify-center gap-x-6 gap-y-2 text-sm text-gray-600">
        {preuvesHero.map((p) => (
          <span class="flex items-center gap-2">
            <i class="ri-checkbox-circle-line text-[var(--blue)]"></i>
            {p}
          </span>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- CE QUE FAIT HERMÈS -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4 bg-white border-y-2 border-black">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12 md:mb-16">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          capacités
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">
          Ce que fait <strong>Hermès</strong>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Pas une démo de labo. Des tâches réelles de PME, avec la validation humaine là où elle compte.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {capacites.map((cap) => (
          <div class="group bg-[var(--main-bg)] border-2 border-black p-6 shadow-[4px_4px_0px_rgba(0,0,0,0.3)] transition-all duration-200 hover:translate-x-[-2px] hover:translate-y-[-2px] hover:shadow-[6px_6px_0px_rgba(0,0,0,0.3)] flex flex-col">
            <div class="w-12 h-12 bg-[var(--blue)] border-2 border-black flex items-center justify-center mb-4 group-hover:bg-[var(--yellow)] transition-colors">
              <i class={`${cap.icon} text-2xl`}></i>
            </div>
            <h3 class="text-lg font-bold mb-2">{cap.titre}</h3>
            <p class="text-gray-600 text-sm leading-relaxed flex-grow">{cap.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- CAS D'USAGE CONCRETS -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4">
    <div class="max-w-5xl mx-auto">
      <div class="text-center mb-12 md:mb-16">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          sur le terrain
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">
          Des exemples <strong>de PME romande</strong>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Voici à quoi ressemble Hermès une fois branché sur un vrai métier.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {casUsage.map((cas) => (
          <div class="bg-white border-2 border-black p-6 shadow-[4px_4px_0px_rgba(0,0,0,0.3)] flex items-start gap-5">
            <div class="w-12 h-12 bg-[var(--violet)] text-white border-2 border-black flex items-center justify-center flex-shrink-0">
              <i class={`${cas.icon} text-xl`}></i>
            </div>
            <div>
              <h3 class="text-base font-bold mb-1">{cas.secteur}</h3>
              <p class="text-gray-600 text-sm leading-relaxed">{cas.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- L'OFFRE — GRILLE DE PRIX (cœur transactionnel) -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4 bg-white border-y-2 border-black">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12 md:mb-16">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          l'offre
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">
          Des <strong>prix affichés</strong>, en CHF
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Pas de devis-mystère à réclamer par e-mail. Tu vois les fourchettes, le format de cadrage et le délai avant de me contacter.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-10 items-stretch">
        {paliers.map((palier) => (
          <div class={`border-black p-6 md:p-8 flex flex-col ${palier.highlight ? "bg-[var(--main-bg)] border-3 shadow-[8px_8px_0px_rgba(0,0,0,1)]" : "bg-[var(--main-bg)] border-2 shadow-[4px_4px_0px_rgba(0,0,0,0.3)]"}`}>
            <span class={`self-start inline-flex items-center gap-2 border-2 border-black px-3 py-1 text-xs font-mono mb-4 ${palier.highlight ? "bg-[var(--yellow)]" : "bg-white"}`}>
              {palier.tag}
            </span>
            <h3 class="text-xl font-bold mb-1">{palier.nom}</h3>
            <p class="text-sm text-gray-500 mb-4">{palier.pour}</p>
            <div class="font-mono font-black text-2xl md:text-3xl mb-6">{palier.prix}</div>
            <ul class="space-y-3 flex-grow">
              {palier.inclus.map((item) => (
                <li class="flex items-start gap-3 text-sm text-gray-700 leading-relaxed">
                  <i class="ri-checkbox-circle-fill text-[var(--blue)] mt-0.5 flex-shrink-0"></i>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>

      <div class="bg-[var(--main-bg)] border-2 border-black p-6 shadow-[4px_4px_0px_rgba(0,0,0,0.3)] flex flex-col md:flex-row md:items-center gap-5">
        <div class="w-12 h-12 bg-[var(--yellow)] border-2 border-black flex items-center justify-center flex-shrink-0">
          <i class="ri-timer-flash-line text-2xl"></i>
        </div>
        <div class="flex-grow">
          <h3 class="font-bold mb-1">Délai de déploiement : 2 à 3 semaines</h3>
          <p class="text-sm text-gray-600 leading-relaxed">
            Entre le cadrage et un premier agent en production sur une tâche précise. On élargit ensuite le périmètre seulement si le gain le justifie.
          </p>
        </div>
        <a
          href="/contact"
          class="bg-[var(--blue)] border-2 border-black px-6 py-3 font-bold shadow-[4px_4px_0px_rgba(0,0,0,1)] transition-all duration-200 hover:bg-[var(--yellow)] hover:shadow-[2px_2px_0px_rgba(0,0,0,1)] hover:translate-x-[2px] hover:translate-y-[2px] flex items-center justify-center gap-2 flex-shrink-0"
        >
          <i class="ri-calendar-check-line"></i>
          Cadrer mon déploiement
        </a>
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- COMMENT ÇA SE PASSE -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12 md:mb-16">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          méthode
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">
          Comment <strong>ça se passe</strong>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Une méthode qui part de tes process réels et garde l'humain aux commandes.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {etapes.map((e) => (
          <div class="bg-white border-2 border-black p-6 shadow-[4px_4px_0px_rgba(0,0,0,0.3)] flex flex-col">
            <div class="font-mono font-black text-3xl text-[var(--blue)] mb-3">{e.step}</div>
            <h3 class="text-lg font-bold mb-2">{e.title}</h3>
            <p class="text-gray-600 text-sm leading-relaxed flex-grow">{e.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- FAQ -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4 bg-white border-y-2 border-black">
    <div class="max-w-3xl mx-auto">
      <div class="text-center mb-12">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          faq
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">Questions fréquentes</h2>
        <p class="text-lg text-gray-600">
          Ce qu'on me demande avant de déployer Hermès.
        </p>
      </div>

      <div class="faq-list space-y-4">
        {faqs.map((faq, index) => (
          <div class="faq-item border-2 border-black bg-white shadow-[3px_3px_0px_rgba(0,0,0,0.3)]" data-faq-item>
            <button
              class="faq-trigger w-full flex items-center justify-between gap-4 p-5 text-left font-medium cursor-pointer transition-colors duration-200 hover:bg-[var(--blue)] hover:bg-opacity-10"
              aria-expanded="false"
              data-faq-trigger
            >
              <span class="flex items-start gap-3">
                <span class="font-mono text-sm text-gray-400 mt-0.5">{String(index + 1).padStart(2, '0')}</span>
                <span class="text-base md:text-lg">{faq.question}</span>
              </span>
              <span class="faq-icon w-8 h-8 border-2 border-black bg-[var(--main-bg)] flex items-center justify-center transition-transform duration-300 flex-shrink-0">
                <i class="ri-add-line text-lg"></i>
              </span>
            </button>
            <div class="faq-content overflow-hidden transition-all duration-300" style="max-height: 0;" data-faq-content>
              <div class="p-6 pt-2 pl-12">
                <p class="text-gray-600 leading-relaxed">{faq.answer}</p>
              </div>
            </div>
          </div>
        ))}
      </div>

      <p class="text-center mt-10 text-gray-500 text-sm">
        Une autre question ? <a href="/contact" class="text-[var(--blue)] font-medium hover:underline">Écris-moi</a>, je réponds sous 24h.
      </p>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- MAILLAGE -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4">
    <div class="max-w-5xl mx-auto">
      <div class="text-center mb-12 md:mb-16">
        <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-500 mb-4">
          <span class="w-8 h-px bg-gray-400"></span>
          aller plus loin
          <span class="w-8 h-px bg-gray-400"></span>
        </span>
        <h2 class="text-3xl md:text-4xl font-normal tracking-tight mb-4">
          Pour creuser le sujet
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {liens.map((lien) => (
          <a
            href={lien.href}
            class="group bg-white border-2 border-black p-6 shadow-[4px_4px_0px_rgba(0,0,0,0.3)] transition-all duration-200 hover:translate-x-[-2px] hover:translate-y-[-2px] hover:shadow-[6px_6px_0px_rgba(0,0,0,0.3)] hover:bg-[var(--main-bg)] flex flex-col"
          >
            <div class="w-12 h-12 bg-[var(--blue)] border-2 border-black flex items-center justify-center mb-4 group-hover:bg-[var(--yellow)] transition-colors">
              <i class={`${lien.icon} text-2xl`}></i>
            </div>
            <h3 class="text-xl font-bold mb-2">{lien.title}</h3>
            <p class="text-gray-600 text-sm leading-relaxed flex-grow">{lien.description}</p>
            <span class="mt-4 pt-4 border-t border-dashed border-gray-300 text-sm text-[var(--blue)] font-medium flex items-center gap-1">
              {lien.cta}
              <i class="ri-arrow-right-line group-hover:translate-x-1 transition-transform"></i>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================================== -->
  <!-- CTA FINAL -->
  <!-- ============================================== -->
  <section class="py-20 md:py-28 px-4 bg-[var(--text)] text-white relative overflow-hidden">
    <div class="absolute inset-0 opacity-[0.07]" aria-hidden="true">
      <div style="background-image: linear-gradient(white 1px, transparent 1px), linear-gradient(90deg, white 1px, transparent 1px); background-size: 40px 40px; width: 100%; height: 100%;"></div>
    </div>
    <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
      <div class="absolute top-0 left-1/4 w-[400px] h-[400px] bg-[var(--blue)] opacity-20 blur-[120px] rounded-full"></div>
      <div class="absolute bottom-0 right-1/4 w-[300px] h-[300px] bg-[var(--violet)] opacity-15 blur-[100px] rounded-full"></div>
    </div>

    <div class="max-w-4xl mx-auto relative z-10 text-center">
      <span class="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-[0.2em] text-gray-400 mb-6">
        <span class="w-8 h-px bg-gray-600"></span>
        prêt ?
        <span class="w-8 h-px bg-gray-600"></span>
      </span>

      <h2 class="text-4xl md:text-6xl font-normal tracking-tight mb-6 leading-[1.05]">
        On regarde ensemble ce qu'Hermès peut t'enlever
      </h2>

      <p class="text-lg md:text-xl text-gray-300 mb-10 max-w-2xl mx-auto leading-relaxed">
        Premier échange gratuit, sans engagement. On identifie les 2-3 tâches qui te mangent le plus de temps, et je te dis franchement si Hermès y répond. Réponse sous 24h.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-8">
        <a
          href="/contact"
          class="bg-[var(--blue)] text-[var(--text)] border-2 border-[var(--blue)] px-8 py-4 text-lg font-bold shadow-[4px_4px_0px_white] transition-all duration-200 hover:bg-[var(--yellow)] hover:border-[var(--yellow)] hover:shadow-[2px_2px_0px_white] hover:translate-x-[2px] hover:translate-y-[2px] flex items-center justify-center gap-2"
        >
          <i class="ri-calendar-check-line"></i>
          Cadrer mon déploiement
        </a>
        <a
          href="mailto:hello@jonlabs.ch"
          class="bg-transparent text-white border-2 border-white px-8 py-4 text-lg font-medium transition-all duration-200 hover:bg-white hover:text-[var(--text)] flex items-center justify-center gap-2 font-mono"
        >
          <i class="ri-mail-line"></i>
          hello@jonlabs.ch
        </a>
      </div>

      <p class="text-sm text-gray-400 flex items-center justify-center gap-2">
        <i class="ri-time-line text-[var(--blue)]"></i>
        Réponse sous 24h · Lun-Ven, 9h-18h (CET) · Facturation en CHF
      </p>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .faq-item[data-open="true"] .faq-icon {
    transform: rotate(45deg);
    background-color: var(--blue);
  }

  section[id] {
    scroll-margin-top: 100px;
  }
</style>

<script>
  const faqItems = document.querySelectorAll('[data-faq-item]');

  faqItems.forEach(item => {
    const trigger = item.querySelector('[data-faq-trigger]');
    const content = item.querySelector('[data-faq-content]') as HTMLElement;

    trigger?.addEventListener('click', () => {
      const isOpen = item.getAttribute('data-open') === 'true';

      faqItems.forEach(otherItem => {
        if (otherItem !== item) {
          otherItem.setAttribute('data-open', 'false');
          const otherContent = otherItem.querySelector('[data-faq-content]') as HTMLElement;
          const otherTrigger = otherItem.querySelector('[data-faq-trigger]');
          if (otherContent) otherContent.style.maxHeight = '0';
          otherTrigger?.setAttribute('aria-expanded', 'false');
        }
      });

      if (isOpen) {
        item.setAttribute('data-open', 'false');
        content.style.maxHeight = '0';
        trigger.setAttribute('aria-expanded', 'false');
      } else {
        item.setAttribute('data-open', 'true');
        content.style.maxHeight = content.scrollHeight + 'px';
        trigger.setAttribute('aria-expanded', 'true');
      }
    });
  });
</script>
