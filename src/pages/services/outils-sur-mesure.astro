---
// Outils & applications sur mesure — sous-page service (blueprint).
// Migrée du brutaliste vers le design system .co-* : sections cadrées, filets fins,
// accent teal unique, Inter Tight × Instrument Serif. Namespace scopé .ou-* (corps .co-root).
// Gabarit-or de référence : src/pages/services/creation-site-web.astro (.cr-*).
//
// HARD-PRESERVE : Schema.org (serviceSchemas/breadcrumb/FAQ), câblage Web3Forms
// (#outils-form, access_key + hidden inputs, tous les name=, honeypot, JS submit),
// prix exacts (1'000 / 2'500 / Sur devis), tous les liens SEO internes
// (/services/automatisation, /blog/automatisation-club-sportif, /blog/valide-idee-business-suisse).

import '../../styles/global.css';
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

// Image hero (dashboard réel)
import dashboardScreen from '../../assets/services/dashboard-custom-screen.webp';

// Schema.org
import { serviceSchemas, SITE_URL } from '../../data/schema';
import { getBreadcrumbSchema, getFAQPageSchema } from '../../lib/seo';

// SEO Data
const seoData = {
    title: "Outils & Applications sur mesure",
    description: "Développement d'applications et dashboards sur mesure pour PME et indépendants. Sortez d'Excel, centralisez vos données. Prototype en 2 semaines."
};

// Schema.org data
const breadcrumb = getBreadcrumbSchema([
    { name: "Accueil", url: SITE_URL },
    { name: "Services", url: `${SITE_URL}/services` },
    { name: "Outils sur mesure", url: `${SITE_URL}/services/outils-sur-mesure` }
]);
const schemas: any[] = [serviceSchemas["outils-sur-mesure"], breadcrumb];

// Pain points data (4 items) - Format conversationnel
const painPoints = [
    {
        icon: "ri-file-excel-2-line",
        quote: "Mon fichier Excel de gestion est devenu un monstre que moi seul comprend...",
        description: "Formules cassées, données incohérentes, impossible à partager avec l'équipe. Ça fait le job mais c'est devenu ingérable."
    },
    {
        icon: "ri-links-line",
        quote: "J'utilise 4 outils différents qui ne se parlent pas, et je ressaisis tout à la main.",
        description: "CRM d'un côté, facturation de l'autre, planning ailleurs. Vous passez votre temps à copier-coller entre les apps."
    },
    {
        icon: "ri-function-line",
        quote: "Le logiciel qu'on m'a vendu fait 200 trucs dont 190 qui ne me servent à rien.",
        description: "Et bien sûr, LA fonction dont vous avez vraiment besoin n'existe pas. Ou elle coûte le double en 'module premium'."
    },
    {
        icon: "ri-settings-4-line",
        quote: "J'ai un process métier spécifique, aucun outil du marché ne correspond vraiment.",
        description: "Votre façon de travailler est unique. Les solutions génériques vous forcent à vous adapter à elles, pas l'inverse."
    }
];

// Methodology steps (5 étapes)
const methodSteps = [
    {
        step: "1",
        title: "Diagnostic",
        description: "On analyse ensemble ton process actuel. Où tu perds du temps ? Qu'est-ce qui coince ? Qu'est-ce qui manque ? Pas de jargon technique, juste des questions concrètes.",
        duration: "1-2h d'échange"
    },
    {
        step: "2",
        title: "Prototype",
        description: "Je construis une première version fonctionnelle — pas un PowerPoint, un vrai outil que tu peux tester. Tu vois concrètement ce que ça donne.",
        duration: "1-2 semaines"
    },
    {
        step: "3",
        title: "Itération",
        description: "Tu testes, tu me dis ce qui marche et ce qui manque. On ajuste. L'outil se construit avec toi, pas dans un coin sans feedback.",
        duration: "Cycles courts"
    },
    {
        step: "4",
        title: "Déploiement",
        description: "Mise en production, formation rapide, documentation. Tu es autonome pour utiliser l'outil au quotidien.",
        duration: "1-2 jours"
    },
    {
        step: "5",
        title: "Évolution",
        description: "Ton business évolue ? L'outil aussi. On peut ajouter des fonctionnalités au fil du temps, sans tout refaire.",
        duration: "Selon besoins"
    }
];

// Differentiators (encart)
const differentiators = [
    { icon: "ri-timer-flash-line", title: "Pas de tunnel de 6 mois", description: "prototype fonctionnel en 1-2 semaines" },
    { icon: "ri-lock-unlock-line", title: "Tu restes propriétaire", description: "ton code, tes données, hébergées où tu veux" },
    { icon: "ri-file-text-line", title: "Pas de dépendance", description: "documentation complète, évolutif même sans moi" },
    { icon: "ri-shield-check-line", title: "Pensé pour durer", description: "stack moderne et maintenable" }
];

// Results (6 items)
const results = [
    { icon: "ri-global-line", title: "Une application web accessible partout", benefit: "Plus de fichier Excel coincé sur un seul ordi" },
    { icon: "ri-user-smile-line", title: "Interface pensée pour toi (et ton équipe)", benefit: "Pas besoin de formation de 3 jours, c'est intuitif" },
    { icon: "ri-database-2-line", title: "Données centralisées et sécurisées", benefit: "Fini les versions contradictoires et les fichiers perdus" },
    { icon: "ri-robot-line", title: "Automatisations intégrées si pertinent", benefit: "Les tâches répétitives se font sans toi" },
    { icon: "ri-expand-diagonal-line", title: "Évolutif selon tes besoins", benefit: "L'outil grandit avec ton activité" },
    { icon: "ri-book-open-line", title: "Documentation + formation incluses", benefit: "Tu es autonome dès le jour 1" }
];

// Tech stack
const techStack = [
    { name: "React / Next.js", description: "Interfaces modernes, rapides, agréables à utiliser", icon: "ri-reactjs-line" },
    { name: "Supabase", description: "Base de données robuste, auth intégrée, temps réel si besoin", icon: "ri-database-2-line" },
    { name: "Tailwind CSS", description: "Design sur mesure, pas de template générique", icon: "ri-palette-line" },
    { name: "Vercel", description: "Hébergement fiable, déploiement automatique", icon: "ri-cloud-line" }
];

// Pricing tiers
const pricingTiers = [
    {
        name: "Dashboard simple",
        price: "1'000",
        priceLabel: "À partir de",
        description: "Tableau de bord pour visualiser et gérer vos données essentielles",
        highlighted: false
    },
    {
        name: "Application métier",
        price: "2'500",
        priceLabel: "À partir de",
        description: "Outil complet avec workflow, automatisations et multi-utilisateurs",
        highlighted: true
    },
    {
        name: "Projet complexe",
        price: "Sur devis",
        priceLabel: "",
        description: "Solution sur mesure pour besoins spécifiques ou intégrations multiples",
        highlighted: false
    }
];

// Pricing factors
const pricingFactors = [
    "Complexité des fonctionnalités",
    "Nombre d'utilisateurs prévus",
    "Intégrations avec d'autres outils",
    "Niveau de personnalisation de l'interface"
];

// FAQs (6 questions)
const faqs = [
    {
        question: "C'est quoi la différence avec un logiciel SaaS du marché ?",
        answer: "Un SaaS, tu t'adaptes à lui. Un outil sur mesure, c'est lui qui s'adapte à toi. Pas de fonctionnalités inutiles, pas de compromis sur ce dont tu as vraiment besoin."
    },
    {
        question: "Et si j'ai déjà des outils en place ?",
        answer: "On peut créer des ponts. Ton outil sur mesure peut se connecter à ton CRM, ta compta, tes fichiers existants. L'objectif c'est de simplifier, pas de tout casser."
    },
    {
        question: "Combien de temps pour avoir quelque chose de fonctionnel ?",
        answer: "Un premier prototype testable en 1-2 semaines. La version complète dépend de la complexité, mais on avance par itérations — tu ne restes jamais des mois sans rien voir."
    },
    {
        question: "Qu'est-ce qui se passe si tu n'es plus disponible dans 2 ans ?",
        answer: "Ton code t'appartient. Je documente tout. Tu peux le faire évoluer avec n'importe quel développeur compétent. Pas de dépendance forcée."
    },
    {
        question: "J'ai une idée mais je ne sais pas si c'est faisable techniquement...",
        answer: "C'est exactement pour ça qu'on commence par un échange. Je te dis ce qui est réaliste, ce qui ne l'est pas, et comment aborder le projet intelligemment.",
        linkText: "Découvre comment valider une idée avant de construire",
        linkUrl: "/blog/valide-idee-business-suisse"
    },
    {
        question: "Je ne suis pas technique, c'est un problème ?",
        answer: "Pas du tout. La plupart de mes clients ne le sont pas. Mon job c'est de traduire ton besoin métier en solution technique — pas de t'assommer de jargon."
    }
];
schemas.push(getFAQPageSchema(faqs));

// Local advantages
const localAdvantages = [
    { icon: "ri-time-line", title: "Même fuseau horaire", description: "Réactivité garantie, réponse dans la journée" },
    { icon: "ri-shield-user-line", title: "Contexte suisse", description: "Compréhension de la nLPD, spécificités légales, multilinguisme" },
    { icon: "ri-server-line", title: "Hébergement local possible", description: "Tes données en Suisse si tes contraintes l'exigent" },
    { icon: "ri-team-line", title: "Réseau local", description: "Si ton projet nécessite d'autres compétences, je connais des gens de confiance" }
];

// Project type options for form
const projectTypes = [
    { value: "dashboard", label: "Dashboard / Tableau de bord" },
    { value: "app-metier", label: "Application métier" },
    { value: "automatisation", label: "Automatisation de process" },
    { value: "autre", label: "Autre / Je ne sais pas" }
];

// Budget options for form
const budgetOptions = [
    { value: "2500-5000", label: "2'500 - 5'000 CHF" },
    { value: "5000-10000", label: "5'000 - 10'000 CHF" },
    { value: "plus-10000", label: "> 10'000 CHF" },
    { value: "ne-sait-pas", label: "Je ne sais pas encore" }
];
---

<Layout title={seoData.title} description={seoData.description} schemas={schemas}>
    <Header />

    <main class="co-root">

        <!-- ══ HERO ══ -->
        <section class="co-sec co-pad co-hero ou-hero" style="padding:64px var(--sec-pad-x) var(--sec-pad-y);">
            <div class="ou-hero-grid">
                <div class="ou-hero-copy">
                    <h1 class="ou-hero-title" data-animate data-delay="0.1">
                        Ton métier est unique. Ton outil devrait l'être <span class="co-ser">aussi.</span>
                    </h1>
                    <p class="ou-hero-lead" data-animate data-delay="0.2">
                        Excel atteint ses limites, les logiciels du marché ne collent pas à ton process, et bricoler
                        entre 5 apps différentes te fait perdre du temps.
                        <strong>Je crée des dashboards et outils métier pensés pour ta façon de travailler</strong> —
                        pas l'inverse.
                    </p>
                    <div class="ou-hero-btns" data-animate data-delay="0.3">
                        <a href="#contact" class="co-btn"><i class="ri-chat-3-line"></i> Discuter de mon projet</a>
                        <a href="#resultats" class="co-btn co-btn--ghost">Voir des exemples <i class="ri-arrow-down-line"></i></a>
                    </div>
                    <p class="ou-hero-loc co-mut" data-animate data-delay="0.4">
                        <i class="ri-map-pin-fill"></i> Basé à Genève · Service en Suisse romande
                    </p>
                </div>

                <div class="ou-hero-media" data-animate="scale" data-delay="0.3">
                    <div class="ou-browser co-frame">
                        <span class="co-x tl"></span><span class="co-x tr"></span>
                        <span class="co-x bl"></span><span class="co-x br"></span>
                        <div class="ou-browser-bar">
                            <span class="ou-browser-dot"></span>
                            <span class="ou-browser-dot"></span>
                            <span class="ou-browser-dot"></span>
                            <span class="ou-browser-url">dashboard.app</span>
                        </div>
                        <img src={dashboardScreen.src} alt="Exemple de dashboard sur mesure — gestion de club sportif avec membres, cotisations et inscriptions" loading="eager" class="ou-browser-img" />
                    </div>
                    <div class="ou-badge ou-badge--tag">Sur mesure</div>
                    <div class="ou-badge ou-badge--speed">1-2 sem.</div>
                </div>
            </div>
        </section>

        <!-- ══ 01 · PROBLÈMES ══ -->
        <section id="problemes" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">01 — Problèmes</p>
            <h2 style="margin-top:var(--space-eyebrow);">Tu te <span class="co-ser">reconnais ?</span></h2>
            <p class="co-mut" style="margin-top:var(--space-title-lead);max-width:58ch;line-height:1.6;">
                Ces situations reviennent souvent chez les PME et indépendants que j'accompagne.
            </p>

            <div class="ou-grid ou-grid--2" data-animate-stagger>
                {painPoints.map((point) => (
                    <div class="ou-card co-frame co-frame--reveal" data-animate-item>
                        <i class={`${point.icon} ou-card-icon`} aria-hidden="true"></i>
                        <p class="ou-card-quote">« {point.quote} »</p>
                        <p class="ou-card-text">{point.description}</p>
                    </div>
                ))}
            </div>

            <div class="ou-note ou-note--accent" data-animate>
                <i class="ri-lightbulb-line"></i>
                <p><strong>Le problème n'est pas que tu es difficile.</strong> C'est que ton métier a des spécificités que les logiciels généralistes ignorent. La solution ? Un outil conçu autour de <strong>ton</strong> fonctionnement.</p>
            </div>
        </section>

        <!-- ══ 02 · MÉTHODE ══ -->
        <section id="methode" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">02 — Méthode</p>
            <h2 style="margin-top:var(--space-eyebrow);">Comment ça se passe ?</h2>
            <p class="co-mut" style="margin-top:var(--space-title-lead);max-width:62ch;line-height:1.6;">
                On part de ton besoin réel, pas d'un cahier des charges fictif. Tu vois un
                <strong>prototype fonctionnel en 1-2 semaines</strong>, pas dans 6 mois.
            </p>

            <div class="ou-steps" data-animate-stagger>
                {methodSteps.map((step, index) => (
                    <div class="ou-step" data-animate-item>
                        <span class="ou-step-num co-index">0{index + 1}</span>
                        <div class="ou-step-body">
                            <h3 class="ou-step-title">{step.title}</h3>
                            <p class="ou-step-text">{step.description}</p>
                            <p class="ou-step-dur"><i class="ri-time-line"></i> {step.duration}</p>
                        </div>
                    </div>
                ))}
            </div>

            <div class="ou-diff co-frame" data-animate>
                <span class="co-x tl"></span><span class="co-x tr"></span>
                <span class="co-x bl"></span><span class="co-x br"></span>
                <h3 class="ou-diff-title">Ce qui change avec Jon Labs</h3>
                <div class="ou-diff-grid">
                    {differentiators.map((diff) => (
                        <div class="ou-diff-item">
                            <i class={`${diff.icon} ou-diff-icon`} aria-hidden="true"></i>
                            <h4 class="ou-diff-name">{diff.title}</h4>
                            <p class="ou-diff-desc">{diff.description}</p>
                        </div>
                    ))}
                </div>
            </div>

            <div class="ou-note" data-animate>
                <p>
                    Si ton besoin c'est surtout de connecter des outils existants,
                    <a href="/services/automatisation" class="ou-inline">découvre mes services d'automatisation</a>.
                </p>
            </div>
        </section>

        <!-- ══ 03 · RÉSULTATS ══ -->
        <section id="resultats" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">03 — Résultats</p>
            <h2 style="margin-top:var(--space-eyebrow);">Ce que tu <span class="co-ser">obtiens.</span></h2>

            <div class="ou-grid ou-grid--3" data-animate-stagger>
                {results.map((result) => (
                    <div class="ou-card co-frame co-frame--reveal" data-animate-item>
                        <i class={`${result.icon} ou-card-icon`} aria-hidden="true"></i>
                        <h3 class="ou-card-title">{result.title}</h3>
                        <p class="ou-card-benefit"><i class="ri-arrow-right-line" aria-hidden="true"></i><span>{result.benefit}</span></p>
                    </div>
                ))}
            </div>

            <div class="ou-case co-frame" data-animate>
                <span class="co-x tl"></span><span class="co-x tr"></span>
                <span class="co-x bl"></span><span class="co-x br"></span>
                <div class="ou-case-head">
                    <i class="ri-trophy-line ou-case-icon" aria-hidden="true"></i>
                    <h3 class="ou-case-title">Exemple concret : dashboard pour club sportif</h3>
                </div>
                <p class="ou-case-text">
                    Un club gérait ses membres, cotisations et présences sur 3 fichiers Excel qui ne se parlaient pas.
                    J'ai créé un dashboard centralisé : vue d'ensemble des membres, suivi des paiements en temps réel,
                    historique des présences. <strong>Le comité a récupéré plusieurs heures par mois</strong> — et
                    surtout, n'importe qui peut reprendre la gestion sans formation.
                </p>
                <a href="/blog/automatisation-club-sportif" class="ou-inline ou-case-link">
                    <i class="ri-article-line"></i> Lire l'article complet <i class="ri-arrow-right-line"></i>
                </a>
            </div>
        </section>

        <!-- ══ 04 · TECHNOLOGIE ══ -->
        <section id="technologie" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">04 — Sous le capot</p>
            <h2 style="margin-top:var(--space-eyebrow);">Technologie moderne</h2>
            <p class="co-mut" style="margin-top:var(--space-title-lead);max-width:62ch;line-height:1.6;">
                Je choisis la technologie en fonction de <strong>ton</strong> projet, pas de mes préférences.
                Mais voici ce avec quoi je travaille le plus souvent.
            </p>

            <div class="ou-grid ou-grid--4" data-animate-stagger="scale">
                {techStack.map((tech) => (
                    <div class="ou-card ou-card--tech co-frame co-frame--reveal" data-animate-item>
                        <i class={`${tech.icon} ou-card-icon`} aria-hidden="true"></i>
                        <h3 class="ou-card-title">{tech.name}</h3>
                        <p class="ou-card-text">{tech.description}</p>
                    </div>
                ))}
            </div>

            <div class="ou-note" data-animate>
                <i class="ri-information-line"></i>
                <p><strong>Pas besoin de comprendre ces termes.</strong> Ce qui compte pour toi : c'est moderne, c'est maintenable, et tes données restent les tiennes.</p>
            </div>
        </section>

        <!-- ══ 05 · PRIX ══ -->
        <section id="prix" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">05 — Investissement</p>
            <h2 style="margin-top:var(--space-eyebrow);">Des ordres de <span class="co-ser">grandeur.</span></h2>
            <p class="co-mut" style="margin-top:var(--space-title-lead);max-width:56ch;">Chaque projet est différent. Voici des repères pour te donner une idée.</p>

            <div class="ou-tiers" data-animate-stagger>
                {pricingTiers.map((tier) => (
                    <div class={`ou-tier ${tier.highlighted ? 'ou-tier--reco' : ''}`} data-animate-item>
                        {tier.highlighted && <span class="co-badge co-badge--solid ou-tier-reco">Courant</span>}
                        <h3 class="ou-tier-name">{tier.name}</h3>
                        <div class="ou-tier-price">
                            {tier.priceLabel && <span class="ou-tier-pricelabel">{tier.priceLabel}</span>}
                            <b>{tier.price}{tier.priceLabel && <span class="ou-tier-cur"> CHF</span>}</b>
                        </div>
                        <p class="ou-tier-desc">{tier.description}</p>
                    </div>
                ))}
            </div>

            <div class="ou-pricenote co-frame" data-animate>
                <span class="co-x tl"></span><span class="co-x tr"></span>
                <span class="co-x bl"></span><span class="co-x br"></span>
                <h3 class="ou-pricenote-title"><i class="ri-information-line"></i> Ce qui influence le budget</h3>
                <ul class="co-list ou-pricenote-grid">
                    {pricingFactors.map((factor) => (<li>{factor}</li>))}
                </ul>
                <p class="ou-note-inline"><strong>Premier échange gratuit</strong> pour cadrer le besoin et te donner une estimation précise.</p>
            </div>
        </section>

        <!-- ══ 06 · FAQ (accordéon natif <details>, zéro JS) ══ -->
        <section id="faq" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">06 — FAQ</p>
            <h2 style="margin-top:var(--space-eyebrow);">Questions fréquentes</h2>

            <div class="ou-faq" style="margin-top:var(--space-block);">
                {faqs.map((faq, index) => (
                    <details open={index === 0}>
                        <summary>
                            {faq.question}
                            <i class="ri-arrow-down-s-line" aria-hidden="true"></i>
                        </summary>
                        <p class="ou-faq-answer">
                            {faq.answer}
                            {faq.linkUrl && <> <a href={faq.linkUrl} class="ou-inline">{faq.linkText}</a></>}
                        </p>
                    </details>
                ))}
            </div>

            <p class="co-mut" style="margin-top:24px;text-align:center;">
                Une autre question ? <a href="#contact" class="ou-inline">Contacte-moi directement</a>, je réponds sous 24h.
            </p>
        </section>

        <!-- ══ 07 · LOCAL ══ -->
        <section id="local" class="co-sec co-pad" style="padding:var(--sec-pad-y) var(--sec-pad-x);">
            <p class="co-eyebrow">07 — Local</p>
            <h2 style="margin-top:var(--space-eyebrow);">Basé à <span class="co-ser">Genève</span>, disponible en Suisse romande.</h2>

            <div class="ou-grid ou-grid--2 ou-local-adv" data-animate-stagger>
                {localAdvantages.map((adv) => (
                    <div class="ou-local-item co-frame" data-animate-item>
                        <i class={`${adv.icon} ou-local-icon`} aria-hidden="true"></i>
                        <div>
                            <h3 class="ou-local-name">{adv.title}</h3>
                            <p class="ou-local-desc">{adv.description}</p>
                        </div>
                    </div>
                ))}
            </div>

            <p class="co-mut" style="margin-top:20px;font-size:13.5px;">
                <i class="ri-information-line"></i> Je travaille principalement à distance, mais je peux me déplacer pour les phases de diagnostic et de formation.
            </p>
        </section>

        <!-- ══ 08 · CONTACT (formulaire Web3Forms — HARD-PRESERVE câblage) ══ -->
        <section id="contact" class="co-sec co-pad" style="padding:64px var(--sec-pad-x);">
            <p class="co-eyebrow">08 — Contact</p>
            <h2 style="margin-top:var(--space-eyebrow);">Un process qui mériterait son propre <span class="co-ser">outil ?</span></h2>
            <p class="co-mut" style="margin-top:var(--space-title-lead);max-width:62ch;line-height:1.6;">
                Premier échange gratuit et sans engagement. On regarde ensemble si un outil sur mesure est la bonne
                réponse — ou si une solution plus simple existe.
            </p>

            <form class="ou-form co-frame" id="outils-form" data-animate>
                <span class="co-x tl"></span><span class="co-x tr"></span>
                <span class="co-x bl"></span><span class="co-x br"></span>

                <!-- Hidden fields for Web3Forms (HARD-PRESERVE) -->
                <input type="hidden" name="access_key" value="3f64c7a1-46b9-4524-8e78-47524896d467">
                <input type="hidden" name="subject" value="Demande outil sur mesure - Jon Labs">
                <input type="hidden" name="from_name" value="Jon Labs - Outils sur mesure">
                <input type="checkbox" name="botcheck" class="ou-honeypot" style="display: none;">

                <h3 class="ou-form-title">Parlons de ton projet</h3>

                <div class="ou-form-grid">
                    <div class="ou-field">
                        <label for="name" class="ou-label"><span class="co-index">01</span> Nom / Prénom <span class="ou-req">*</span></label>
                        <input type="text" id="name" name="name" required placeholder="Jean Dupont" class="ou-input" />
                    </div>

                    <div class="ou-field">
                        <label for="email" class="ou-label"><span class="co-index">02</span> Email <span class="ou-req">*</span></label>
                        <input type="email" id="email" name="email" required placeholder="vous@exemple.ch" class="ou-input" />
                    </div>

                    <div class="ou-field">
                        <label for="phone" class="ou-label"><span class="co-index">03</span> Téléphone (optionnel)</label>
                        <input type="tel" id="phone" name="phone" placeholder="+41 79 123 45 67" class="ou-input" />
                    </div>

                    <div class="ou-field">
                        <label for="project-type" class="ou-label"><span class="co-index">04</span> Type de projet <span class="ou-req">*</span></label>
                        <div class="ou-select-wrap">
                            <select id="project-type" name="project_type" required class="ou-select">
                                <option value="" disabled selected>Choisir...</option>
                                {projectTypes.map((type) => (<option value={type.value}>{type.label}</option>))}
                            </select>
                            <i class="ri-arrow-down-s-line ou-select-caret" aria-hidden="true"></i>
                        </div>
                    </div>

                    <div class="ou-field ou-field--full">
                        <label for="message" class="ou-label"><span class="co-index">05</span> Décris ton besoin <span class="ou-req">*</span></label>
                        <textarea id="message" name="message" rows="4" required placeholder="Quel problème veux-tu résoudre ? Quels outils utilises-tu actuellement ? Qu'est-ce qui ne fonctionne pas ?" class="ou-textarea"></textarea>
                    </div>

                    <div class="ou-field">
                        <label for="budget" class="ou-label"><span class="co-index">06</span> Budget approximatif</label>
                        <div class="ou-select-wrap">
                            <select id="budget" name="budget" class="ou-select">
                                <option value="" disabled selected>Choisir...</option>
                                {budgetOptions.map((option) => (<option value={option.value}>{option.label}</option>))}
                            </select>
                            <i class="ri-arrow-down-s-line ou-select-caret" aria-hidden="true"></i>
                        </div>
                    </div>

                    <div class="ou-field">
                        <label for="source" class="ou-label"><span class="co-index">07</span> Comment m'as-tu trouvé ? (optionnel)</label>
                        <input type="text" id="source" name="source" placeholder="Google, recommandation, LinkedIn..." class="ou-input" />
                    </div>

                    <div class="ou-field ou-field--full">
                        <label class="ou-consent">
                            <input type="checkbox" name="consent" required class="ou-checkbox" />
                            <span>Tes données sont utilisées uniquement pour répondre à ta demande. Pas de spam, promis. <span class="ou-req">*</span></span>
                        </label>
                    </div>

                    <div class="ou-field ou-field--full">
                        <button type="submit" class="co-btn submit-btn ou-submit">
                            <span>Envoyer ma demande</span>
                            <i class="ri-send-plane-line"></i>
                        </button>
                    </div>
                </div>
            </form>

            <div class="ou-form-after" data-animate>
                <div class="ou-trust">
                    <span><i class="ri-time-line"></i> Réponse sous 24h</span>
                    <span><i class="ri-gift-line"></i> Premier échange gratuit</span>
                    <span><i class="ri-file-list-3-line"></i> Devis clair et détaillé</span>
                </div>
                <div class="ou-alt">
                    <p class="co-mut">Tu préfères réserver un créneau directement ?</p>
                    <a href="https://cal.com/jonathan-vouilloz/appel-de-bienvenue" target="_blank" rel="noopener noreferrer" class="ou-inline"><i class="ri-calendar-line"></i> Prendre rendez-vous sur Cal.com</a>
                </div>
                <div class="ou-alt">
                    <p class="co-mut">Besoin d'une réponse rapide ?</p>
                    <a href="mailto:contact@jonlabs.ch" class="ou-inline">contact@jonlabs.ch</a>
                </div>
                <p class="ou-note-inline" style="text-align:center;">
                    <strong>Pas encore prêt ?</strong> Découvre
                    <a href="/blog/automatisation-club-sportif" class="ou-inline">comment j'ai aidé des clubs sportifs à sortir de l'enfer Excel</a>.
                </p>
            </div>
        </section>

    </main>

    <Footer />
</Layout>

<style is:global>
    /* ══ OUTILS SUR MESURE — styles .ou-* (namespace scopé, corps .co-root) ══ */

    /* ── Hero ── */
    .ou-hero-grid { display: grid; grid-template-columns: 1.05fr 0.95fr; gap: 48px; align-items: center; }
    .ou-hero-title { font-size: clamp(2.2rem, 5vw, 3.4rem); }
    .co-root .ou-hero-lead { margin-top: 18px; max-width: 52ch; font-size: 1.075rem; line-height: 1.65; color: var(--muted); }
    .ou-hero-lead strong { color: var(--ink); font-weight: 600; }
    .ou-hero-btns { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 28px; }
    .ou-hero-btns .co-btn i { transition: transform .18s ease-out; }
    .ou-hero-btns .co-btn:hover i { transform: translateY(2px); }
    .co-root .ou-hero-loc { margin-top: 22px; font-size: 13.5px; display: inline-flex; align-items: center; gap: 6px; }
    .ou-hero-loc i { color: var(--accent); }

    /* app/dashboard mockup */
    .ou-hero-media { position: relative; }
    .ou-browser { position: relative; background: var(--surface); border: 1px solid var(--line-2); }
    .ou-browser-bar { display: flex; align-items: center; gap: 8px; padding: 11px 14px; border-bottom: 1px solid var(--line); background: var(--bg); }
    .ou-browser-dot { width: 9px; height: 9px; border-radius: 999px; background: var(--line-2); flex: none; }
    .ou-browser-url { flex: 1; text-align: center; font-size: 12px; color: var(--muted); font-variant-numeric: tabular-nums; }
    .ou-browser-img { display: block; width: 100%; height: auto; }
    .ou-badge { position: absolute; z-index: 4; background: var(--surface); border: 1px solid var(--line-2); padding: var(--pad-pill); font-size: 12px; font-weight: 600; }
    .ou-badge--tag { top: -16px; left: -14px; color: var(--accent); }
    .ou-badge--speed { bottom: -16px; right: -14px; background: var(--accent); border-color: var(--accent); color: #fff; }

    /* ── Cartes génériques (problèmes / résultats / tech) ── */
    .ou-grid { display: grid; gap: 16px; margin-top: var(--space-block); }
    .ou-grid--2 { grid-template-columns: repeat(2, 1fr); }
    .ou-grid--3 { grid-template-columns: repeat(3, 1fr); }
    .ou-grid--4 { grid-template-columns: repeat(4, 1fr); }
    .ou-card {
        position: relative; display: flex; flex-direction: column; gap: 10px; padding: var(--pad-card);
        background: var(--surface); border: 1px solid var(--line-2);
        transition: box-shadow .18s ease-out, transform .18s ease-out;
    }
    .ou-card:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(20,22,26,.07); }
    .ou-card--tech { text-align: center; align-items: center; padding: 22px 18px; }
    .ou-card-icon { font-size: 24px; color: var(--accent); line-height: 1; }
    .ou-card-title { font-size: 1.1rem; }
    .ou-card-text { font-size: 14px; line-height: 1.55; color: var(--muted); }
    .ou-card-quote { font-family: "Instrument Serif", Georgia, serif; font-style: italic; font-size: 1.15rem; line-height: 1.4; color: var(--ink); }
    .co-root .ou-card-benefit { margin-top: auto; padding-top: 4px; font-size: 14px; line-height: 1.5; color: var(--muted); display: flex; align-items: flex-start; gap: 8px; }
    .ou-card-benefit i { color: var(--accent); margin-top: 2px; flex: none; }

    /* ── Notes / callouts ── */
    .ou-note { display: flex; align-items: flex-start; gap: 12px; margin-top: 20px; padding: 18px 22px; border: 1px solid var(--line-2); background: var(--surface); }
    .ou-note i { color: var(--accent); font-size: 20px; flex: none; margin-top: 1px; }
    .co-root .ou-note p { font-size: 14.5px; line-height: 1.6; color: var(--muted); }
    .ou-note--accent { border-color: var(--accent); background: rgba(0,168,125,0.05); }
    .ou-note-inline { margin-top: 14px !important; font-size: 14px; line-height: 1.6; color: var(--muted); }
    .ou-inline { color: var(--accent); text-decoration: underline; text-underline-offset: 2px; font-weight: 500; }
    .ou-inline:hover { color: var(--ink); }
    .ou-note strong, .ou-note-inline strong { color: var(--ink); font-weight: 600; }

    /* ── Étapes méthode ── */
    .ou-steps { display: flex; flex-direction: column; margin-top: var(--space-block); border-top: 1px solid var(--line); }
    .ou-step { display: grid; grid-template-columns: 56px 1fr; gap: 20px; padding: 22px 0; border-bottom: 1px solid var(--line); }
    .ou-step-num { font-size: 20px; font-weight: 600; }
    .ou-step-title { font-size: 1.15rem; }
    .co-root .ou-step-text { margin-top: 6px; font-size: 14.5px; line-height: 1.55; color: var(--muted); max-width: 68ch; }
    .co-root .ou-step-dur { margin-top: var(--space-title-lead); font-size: 13px; color: var(--muted); display: inline-flex; align-items: center; gap: 6px; }
    .ou-step-dur i { color: var(--accent); }

    /* ── Encart différenciateurs ── */
    .ou-diff { position: relative; margin-top: var(--space-block); padding: var(--pad-panel); background: var(--surface); border: 1px solid var(--line-2); }
    .ou-diff-title { text-align: center; font-size: 1.25rem; }
    .ou-diff-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 28px; margin-top: 28px; }
    .ou-diff-item { text-align: center; }
    .ou-diff-icon { font-size: 26px; color: var(--accent); }
    .ou-diff-name { margin-top: var(--space-title-lead); font-size: 15px; font-weight: 600; }
    .co-root .ou-diff-desc { margin-top: 4px; font-size: 13px; line-height: 1.45; color: var(--muted); }

    /* ── Étude de cas ── */
    .ou-case { position: relative; margin-top: 28px; padding: 32px 36px; background: var(--surface); border: 1px solid var(--line-2); display: flex; flex-direction: column; gap: 14px; }
    .ou-case-head { display: flex; align-items: center; gap: 12px; }
    .ou-case-icon { font-size: 26px; color: var(--accent); flex: none; }
    .ou-case-title { font-size: 1.2rem; }
    .co-root .ou-case-text { font-size: 14.5px; line-height: 1.6; color: var(--muted); max-width: 72ch; }
    .ou-case-text strong { color: var(--ink); font-weight: 600; }
    .ou-case-link { display: inline-flex; align-items: center; gap: 8px; font-size: 14px; }

    /* ── Tarifs ── */
    .ou-tiers { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; align-items: start; margin-top: 36px; }
    .ou-tier { position: relative; display: flex; flex-direction: column; gap: 16px; padding: 30px 26px; background: var(--surface); border: 1px solid var(--line-2); }
    .ou-tier--reco { border-color: var(--accent); box-shadow: 0 0 0 1px var(--accent); }
    .ou-tier-reco { position: absolute; top: -12px; left: 26px; z-index: 4; }
    .ou-tier-name { font-size: 1.15rem; }
    .ou-tier-price { display: flex; align-items: baseline; gap: 6px; flex-wrap: wrap; }
    .ou-tier-pricelabel { font-size: 13px; color: var(--muted); width: 100%; }
    .ou-tier-price b { font-size: 2.3rem; font-weight: 700; letter-spacing: -0.03em; line-height: 1; }
    .ou-tier-cur { font-size: 15px; color: var(--muted); font-weight: 500; }
    .co-root .ou-tier-desc { font-size: 14px; line-height: 1.55; color: var(--muted); }

    .ou-pricenote { position: relative; margin-top: 28px; padding: var(--pad-encart); background: var(--surface); border: 1px solid var(--line-2); }
    .ou-pricenote-title { font-size: 1.1rem; display: inline-flex; align-items: center; gap: 8px; }
    .ou-pricenote-title i { color: var(--accent); }
    .ou-pricenote-grid { margin-top: 18px; display: grid; grid-template-columns: 1fr 1fr; gap: 8px 40px; }
    .ou-pricenote-grid li { font-size: 14px; }

    /* ── FAQ (accordéon natif <details> — smooth via ::details-content, zéro JS) ── */
    .ou-faq { border-top: 1px solid var(--line); }
    .ou-faq details { border-bottom: 1px solid var(--line); }
    .ou-faq summary {
        list-style: none; cursor: pointer; padding: 20px 0;
        display: flex; justify-content: space-between; align-items: center; gap: 20px;
        font-size: 1.1rem; font-weight: 600; letter-spacing: -0.01em; color: var(--ink);
    }
    .ou-faq summary::-webkit-details-marker { display: none; }
    .ou-faq summary i { transition: transform .2s ease-out; color: var(--muted); font-size: 22px; flex: none; }
    .ou-faq details[open] summary i { transform: rotate(180deg); }
    .co-root .ou-faq-answer { padding: 0 0 22px; color: var(--muted); line-height: 1.6; max-width: 68ch; }
    .ou-faq details::details-content {
        block-size: 0; overflow: hidden;
        transition: block-size .28s ease-out, content-visibility .28s ease-out allow-discrete;
    }
    .ou-faq details[open]::details-content { block-size: auto; }

    /* ── Local ── */
    .ou-local-adv { align-items: stretch; }
    .ou-local-item { position: relative; display: flex; align-items: flex-start; gap: 14px; padding: 18px; background: var(--surface); border: 1px solid var(--line-2); }
    .ou-local-icon { font-size: 22px; color: var(--accent); flex: none; margin-top: 2px; }
    .ou-local-name { font-size: 15px; font-weight: 600; }
    .co-root .ou-local-desc { margin-top: 3px; font-size: 13.5px; line-height: 1.5; color: var(--muted); }

    /* ── Formulaire (Web3Forms restylé, câblage préservé) ── */
    .ou-form { position: relative; margin-top: var(--space-block); padding: 34px; background: var(--surface); border: 1px solid var(--line-2); }
    .ou-form-title { font-size: 1.25rem; text-align: left; margin-bottom: 22px; }
    .ou-form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    .ou-field { display: flex; flex-direction: column; gap: 8px; }
    .ou-field--full { grid-column: 1 / -1; }
    .ou-label { display: flex; align-items: baseline; gap: 8px; font-size: 13px; font-weight: 600; color: var(--ink); }
    .ou-label .co-index { font-size: 11px; }
    .ou-req { color: var(--accent); }
    .ou-input, .ou-select, .ou-textarea {
        width: 100%; font-family: "Inter Tight", ui-sans-serif, sans-serif; font-size: 15px; color: var(--ink);
        background: var(--bg); border: 1px solid var(--line-2); padding: var(--pad-chip);
        transition: border-color .18s ease-out, box-shadow .18s ease-out;
    }
    .ou-input::placeholder, .ou-textarea::placeholder { color: var(--muted); opacity: .7; }
    .ou-input:focus, .ou-select:focus, .ou-textarea:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px rgba(0,168,125,.15); }
    .ou-textarea { resize: vertical; min-height: 120px; line-height: 1.5; }
    .ou-select-wrap { position: relative; }
    .ou-select { appearance: none; -webkit-appearance: none; cursor: pointer; padding-right: 40px; }
    .ou-select-caret { position: absolute; right: 14px; top: 50%; transform: translateY(-50%); font-size: 18px; color: var(--muted); pointer-events: none; }
    .ou-consent { display: flex; align-items: flex-start; gap: 10px; font-size: 13px; line-height: 1.5; color: var(--muted); cursor: pointer; }
    .ou-consent:hover { color: var(--ink); }
    .ou-checkbox { margin-top: 2px; width: 16px; height: 16px; flex: none; accent-color: var(--accent); cursor: pointer; }
    .ou-submit { width: 100%; justify-content: center; padding: 14px 20px; font-size: 16px; }
    .submit-btn.loading { pointer-events: none; opacity: 0.7; }
    .submit-btn.loading i { animation: ouSpin 1s linear infinite; }
    @keyframes ouSpin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

    /* ── Après-formulaire ── */
    .ou-form-after { margin-top: 28px; display: flex; flex-direction: column; gap: 18px; align-items: center; text-align: center; }
    .ou-trust { display: flex; flex-wrap: wrap; justify-content: center; gap: 24px; font-size: 13.5px; color: var(--muted); }
    .ou-trust i { color: var(--accent); margin-right: 4px; }
    .ou-alt { display: flex; flex-direction: column; gap: 4px; }
    .ou-alt .ou-inline { display: inline-flex; align-items: center; gap: 6px; justify-content: center; }

    /* ── Responsive ── */
    @media (max-width: 960px) {
        .ou-hero-grid { grid-template-columns: 1fr; gap: 40px; }
        .ou-hero-media { max-width: 520px; }
        .ou-diff-grid { grid-template-columns: repeat(2, 1fr); }
        .ou-grid--4 { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 720px) {
        .ou-grid--2, .ou-grid--3, .ou-grid--4, .ou-tiers, .ou-form-grid, .ou-pricenote-grid { grid-template-columns: 1fr; }
        .ou-diff-grid { grid-template-columns: 1fr 1fr; }
        .ou-step { grid-template-columns: 44px 1fr; gap: 14px; }
    }
    @media (prefers-reduced-motion: reduce) {
        .ou-card, .ou-inline, .ou-faq summary i, .ou-hero-btns .co-btn i { transition: none; }
        .submit-btn.loading i { animation: none; }
    }
</style>

<script>
    // Form submission — HARD-PRESERVE (câblage Web3Forms inchangé)
    const form = document.getElementById('outils-form') as HTMLFormElement;
    const submitBtn = form?.querySelector('.submit-btn') as HTMLButtonElement;
    const btnText = submitBtn?.querySelector('span');
    const btnIcon = submitBtn?.querySelector('i');

    form?.addEventListener('submit', async (e) => {
        e.preventDefault();

        if (submitBtn) {
            submitBtn.classList.add('loading');
            submitBtn.disabled = true;
            if (btnText) btnText.textContent = 'Envoi en cours...';
            if (btnIcon) btnIcon.className = 'ri-loader-4-line';
        }

        try {
            const formData = new FormData(form);
            const response = await fetch('https://api.web3forms.com/submit', {
                method: 'POST',
                body: formData
            });

            const result = await response.json();

            if (result.success) {
                alert('Merci ! Ta demande a bien été envoyée. Je te réponds sous 24h.');
                form.reset();
            } else {
                alert('Erreur lors de l\'envoi. Réessaie ou écris-moi directement à contact@jonlabs.ch');
            }
        } catch (error) {
            alert('Erreur de connexion. Réessaie.');
        } finally {
            if (submitBtn) {
                submitBtn.classList.remove('loading');
                submitBtn.disabled = false;
                if (btnText) btnText.textContent = 'Envoyer ma demande';
                if (btnIcon) btnIcon.className = 'ri-send-plane-line';
            }
        }
    });
</script>
