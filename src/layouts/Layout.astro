---
import SEO from '../components/SEO.astro';
import type { SchemaType, JSONLDSchema } from '../lib/seo';
// Design system blueprint (tokens .co-root/.co-chrome + primitives .co-*).
// Global : le chrome blueprint (Header/Footer) l'utilise sur toutes les pages.
import '../styles/blueprint.css';

interface Props {
	title?: string;
	description?: string;
	ogImage?: string;
	ogType?: 'website' | 'article';
	// NEW: Array of schemas (preferred)
	schemas?: Array<{ type: SchemaType; data?: any }> | JSONLDSchema[];
	// Legacy: Single schema (backward compatibility)
	schema?: SchemaType;
	schemaData?: Record<string, any>;
	article?: {
		publishedTime: Date;
		modifiedTime?: Date;
		author: string;
		section?: string;
		tags?: string[];
	};
	robots?: string;
}

const {
	title = "Développeur web freelance à Genève",
	description = "Développement web, automatisation, applications. De l'idée au produit fini, j'accompagne entrepreneurs et petites équipes dans leurs projets digitaux.",
	ogImage = "/og-image.png",
	ogType = "website",
	schemas,
	schema,
	schemaData,
	article,
	robots
} = Astro.props;

// Suffixe de marque centralisé : retire toute marque héritée en fin de titre
// (Jon Labs / Jonlabs / Jon Labs Genève, séparée par | – — -) puis ré-appose la
// marque canonique. Idempotent, s'applique à toutes les pages passant par ce Layout
// (y compris blog & guides, sans marque à la source).
//
// Le suffixe est CONDITIONNEL : " | Jonlabs" coûte 10 caractères, et Google tronque
// le titre en SERP autour de 60. Quand les deux ne tiennent pas ensemble, c'est le
// contenu du titre qui gagne — un titre coupé au milieu d'un mot coûte plus de clics
// que l'absence de marque. Budget : 50 caractères à la source (50 + 10 = 60).
const BRAND = "Jonlabs";
const BRAND_SUFFIX_MAX_BASE = 50;
const baseTitle = title.replace(/\s*[|–—-]\s*Jon\s?Labs(?:\s+Genève)?\s*$/i, "").trim();
const fullTitle =
	baseTitle.length <= BRAND_SUFFIX_MAX_BASE ? `${baseTitle} | ${BRAND}` : baseTitle;
---

<!doctype html>
<html lang="fr">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />

		<!-- Preconnect pour performances -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />

		<!-- Fonts blueprint (pairing 04) - Inter Tight × Instrument Serif, chargement non-bloquant -->
		<link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter+Tight:ital,wght@0,300..700;1,300..700&family=Instrument+Serif:ital@0;1&display=swap" />
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter+Tight:ital,wght@0,300..700;1,300..700&family=Instrument+Serif:ital@0;1&display=swap" media="print" onload="this.media='all'" />

		<!-- Remix Icon - chargement différé -->
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@4.6.0/fonts/remixicon.css" media="print" onload="this.media='all'" />

		<!-- Fallback pour navigateurs sans JS -->
		<noscript>
			<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter+Tight:ital,wght@0,300..700;1,300..700&family=Instrument+Serif:ital@0;1&display=swap" />
			<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@4.6.0/fonts/remixicon.css" />
		</noscript>

		<!-- Favicons -->
		<link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg" />
		<link rel="icon" type="image/png" sizes="96x96" href="/favicon/favicon-96x96.png" />
		<link rel="icon" type="image/x-icon" href="/favicon/favicon.ico" />
		<link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
		<link rel="manifest" href="/favicon/site.webmanifest" />
		<meta name="theme-color" content="#F5F4F0" />

		<!-- Anti-flash des animations au scroll. Script INLINE volontairement :
		     le CSS met `[data-animate]` a opacity:0 et seul le moteur
		     (src/scripts/animations.ts, charge en module) les revele. Si ce
		     bundle ne charge pas — erreur reseau, dep Vite perimee, navigateur
		     qui refuse le module — la page reste BLANCHE. Ce marqueur retire la
		     regle au bout de 3s ; quand le moteur tourne, il a deja pose des
		     styles INLINE sur les elements, qui survivent au retrait. -->
		<script is:inline>
			document.documentElement.classList.add("js-anim");
			setTimeout(function () {
				document.documentElement.classList.remove("js-anim");
			}, 3000);
		</script>

		<meta name="generator" content={Astro.generator} />
		<link rel="alternate" type="application/rss+xml" title="Blog Jonlabs" href="/rss.xml" />

		<SEO
			title={fullTitle}
			description={description}
			ogImage={ogImage}
			ogType={ogType}
			schemas={schemas}
			schema={schema}
			schemaData={schemaData}
			article={article}
			robots={robots}
		/>

		<title>{fullTitle}</title>
	</head>
	<body>
		<slot />
		<script>
			import '../scripts/animations';
		</script>
	</body>
</html>

<style>

:root{
	/* ------------------------------------------------------------
	   ALIAS LEGACY restant : --accent (wash teal), surchargé en scope
	   .co-root par blueprint.css (#00A87D). Les autres alias mensongers
	   (--main-bg/--text/--blue/--yellow/--violet/--orange/--cyan) ont
	   été migrés vers les tokens @theme de global.css puis retirés.
	   ------------------------------------------------------------ */
	--accent: var(--color-accent-soft);     /* wash teal (soulignements legacy hors .co-root) */
}

html,
body {
	margin: 0;
	width: 100%;
	min-height: 100%;
	background-color: var(--color-bg);
	color: var(--color-ink);
	font-family: var(--font-sans);
	scroll-behavior: smooth;
}


</style>
