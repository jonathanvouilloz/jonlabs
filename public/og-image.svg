<svg width="1200" height="630" viewBox="0 0 1200 630" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#0F172A"/>

  <!-- Grid pattern -->
  <defs>
    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
      <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#1E293B" stroke-width="1"/>
    </pattern>
  </defs>
  <rect width="1200" height="630" fill="url(#grid)"/>

  <!-- Accent circle -->
  <circle cx="200" cy="315" r="180" fill="#00D9A3" opacity="0.1"/>

  <!-- Logo text -->
  <text x="600" y="280" font-family="Plus Jakarta Sans, sans-serif" font-size="72" font-weight="700" fill="#FFFFFF" text-anchor="middle">
    Jon Labs
  </text>

  <!-- Tagline -->
  <text x="600" y="360" font-family="Plus Jakarta Sans, sans-serif" font-size="28" fill="#94A3B8" text-anchor="middle">
    Solutions Digitales Sur-Mesure
  </text>

  <!-- Accent line -->
  <rect x="500" y="400" width="200" height="4" fill="#00D9A3"/>

  <!-- Location -->
  <text x="600" y="480" font-family="Plus Jakarta Sans, sans-serif" font-size="20" fill="#64748B" text-anchor="middle">
    Genève, Suisse
  </text>
</svg>
